[pytest]
pythonpath = .
# The repo root holds output_full/ (~2.47M corpus files) and output*/ scratch trees.
# Without this, a bare `pytest` walks them during collection and never finishes.
norecursedirs = output* .superpowers .git __pycache__ .pytest_cache data docs
